{
  "name": "EduVillage",
  "image": "mcr.microsoft.com/devcontainers/typescript-node:20",
  "features": {},
  "forwardPorts": [4000, 5173],
  "portsAttributes": {
    "4000": { "label": "API", "onAutoForward": "openBrowser", "protocol": "http" },
    "5173": { "label": "Frontend", "onAutoForward": "openBrowser", "protocol": "http" }
  },
  "postCreateCommand": "corepack enable && corepack prepare pnpm@latest --activate && node -v && npm -v && nvm --version && pnpm -v || true"
}
