{
  "name": "prpl-backend-fastify",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "fastify": "^4.27.0",
    "@fastify/cors": "^10.0.0",
    "@prisma/client": "5.22.0"
  },
  "devDependencies": {
    "prisma": "5.22.0"
  },
  "scripts": {
    "dev": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy"
  },
  "postinstall": "prisma generate"
}
